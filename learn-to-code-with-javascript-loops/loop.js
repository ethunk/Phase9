let i = 0;
let count = 3;
let phrase = 'Repetition is the key to learning.';

while (i < count) {                     // (3 < 3) => false
  console.log('#' + i + ' ' + phrase);  // "#2 Repetition is the key to learning."
  i++;                                  // i = 3
}

// Code execution moves past the loop.
//
//
